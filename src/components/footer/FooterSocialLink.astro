---
import { Image } from "astro:assets";

const { image, classes, ...attrs } = Astro.props;
---

<a class:list={["hover:opacity-90 active:opacity-80", classes]} {...attrs} target="_blank">
  <Image src={image} alt="social icon" />
</a>
